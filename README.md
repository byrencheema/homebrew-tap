# byrencheema/tap

A Homebrew tap. It is empty at the moment.

`airstats` used to live here and now ships from the main Homebrew cask repository:

```sh
brew install --cask airstats
```

Copies installed from this tap follow it there on their next `brew upgrade`, through
the `tap_migrations.json` in this repository.
